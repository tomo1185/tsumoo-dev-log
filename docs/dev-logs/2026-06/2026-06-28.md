# 2026年6月28日（日）

## 今日やったこと

- **TSUMOO-23 達成状況・振り返り画面実装**を続けた
  - 達成率の表示形式を、件数ベースと予定時間ベースで切り替えられる状態にした
  - 日別のふり返りでも、選択中の表示形式に合わせて `完了数 / 対象数` と `完了分 / 対象分（予定時間）` を出し分ける形にした
  - 達成率の色を、100% / 80%以上 / 60%以上 / 30%以上 / 0%や対象なしで段階的に見分けられるようにした
- 直近7日間グラフをさらに調整した
  - カード名を `過去7日間の完了タスク` に変更した
  - 予定時間と実績時間の線を切り替えられるようにし、もう一方の線は薄く残す形にした
  - 未入力がある日は、ホバー内に未入力件数を表示し、丸ポイントにも控えめな警告リングを付けた
  - SP では、切り替えボタンと凡例が横並びで見やすくなるように調整した
  - 直近7日間の完了件数と予定時間合計の文言が、SP でも読みやすくなるように整えた
- 日別のふり返りを、分析として読める形に寄せた
  - `状態` ではなく `ひとこと分析` として、数値を先に置いたメッセージを表示する方針にした
  - 件数ベースと予定時間ベースで、分析文言の優先度を分けた
  - 未入力がある場合は、ユーザーを責める文ではなく、参考値として見ればよいことが伝わる文言にした
  - PC では日付、曜日、完了数 / 対象数、達成率、ひとこと分析の列バランスを調整した
  - SP では、日付、曜日、進捗、達成率、分析メッセージが自然に読める3段構成にした
- 日別詳細の導線を整理した
  - 詳細を開いたときの重複メッセージをやめ、対象日のタスク一覧を別タブで開くリンクに変更した
  - `/app/dashboard?taskDate=YYYY-MM-DD&taskFilter=all` を使い、ダッシュボードのタスク一覧を対象日付 + すべて表示で開けるようにした
  - リンクは通常本文ではなく、水色系でリンクとして分かる見た目にした
- 関連するドキュメントを更新した
  - 振り返り機能実装ガイドに、現在の画面構成、集計方針、レスポンシブ表示、日別メッセージの考え方を反映した
  - ひとこと分析の文言検討メモを作成し、後から見直せるようにした

## ロードマップ

<div class="roadmap-snapshot">
  <div>
    <p class="snapshot-label">フェーズ1: タスク管理MVP</p>
    <p class="snapshot-progress">進捗: 40 / 44件</p>
  </div>
  <div class="progress-track"><span style="width: 91%"></span></div>
</div>

### 今日対応したタスク

- <span class="status-badge doing">進行中</span> **TSUMOO-23** 達成状況・振り返り画面実装

### 次に取り組むタスク

- 振り返りメモの保存・表示方針を整理する
- 日別詳細のメモ入力欄を、実データとして扱えるようにする
- TSUMOO-23 の残り範囲を確認し、完了条件に向けたテストと画面確認を進める

## 考えたこと

今日は、「ひとこと分析」の言い方をかなり詰めました。

達成率を評価するよりも、数字をもとに状況をそっと知らせる方が、毎日使う画面として続けやすいと感じました。

## 決めたこと

- 日別の文言は、数字を先に置き、その後に短いコメントを続ける
- 件数ベースと予定時間ベースで、ひとこと分析の内容を出し分ける
- 未入力がある場合は、入力を促しすぎず、参考値として見ればよいことを伝える
- 詳細を開いたときは、ひとこと分析を重複表示せず、対象日のタスク一覧へのリンクを置く
- TSUMOO-23 はまだ振り返りメモ実装が残っているため、ロードマップ上は進行中として扱う

## 次にやること

- 振り返りメモの実装に入る
- メモの保存先、API、表示タイミング、日別集計との関係を整理する
- 実装後に、振り返り機能実装ガイドをさらに更新する
